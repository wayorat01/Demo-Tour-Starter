# Changelog — มีนาคม 2026

### 2026-03-31 — [feat] เพิ่ม Block ใหม่ WOW ServiceByType + เปลี่ยนชื่อ Block

**เพิ่ม Layout Block ใหม่: WOW ServiceByType**
- สร้าง Block ใหม่ชื่อ `WOW ServiceByType` สำหรับรองรับสินค้าท่องเที่ยว 3 ประเภท: **บัตรเข้าชม** (admission), **เรือสำราญ** (cruise), **รถเช่า** (car_rental)
- ทั้ง 3 ประเภทใช้ Layout/Style เดียวกัน (reuse TourCard 1-6 ทั้ง 6 รูปแบบ) แต่แยก Product Type และ Settings
- ข้อมูลใช้ระบบ Manual Items Array ก่อน — รอเชื่อม API ในอนาคต
- แต่ละ Item รองรับ: รูปปก, ชื่อสินค้า, คำอธิบาย, สถานที่, ระยะเวลา, ราคาเริ่มต้น/ราคาเดิม, Badge, ลิงก์รายละเอียด
- รองรับ Display Mode: Slide / Show All พร้อม Slider Settings (autoplay, loop, delay)

**เปลี่ยนชื่อ Block ใน Admin Panel:**
- `WOW TourType` → **WOW TourByType**
- Design Version Labels: `WowTour Tour Card 1-6` → **WOW Tour Card 1-6**
- ServiceByType Design Version Labels: **WOW Service Card 1-6**

**TypeScript / Build Fixes:**
- แก้ TypeScript error ใน `PostHero/index.tsx` และ `generateMeta.ts` (property `image` ไม่ตรง type หลัง payload type regeneration)
- ลบไฟล์ temp scripts (`tmp-*.ts`) ที่ค้างอยู่ใน root

**ไฟล์ที่เกี่ยวข้อง:**
- `src/blocks/ProductCard/config.ts` (**NEW**), `src/blocks/ProductCard/Component.tsx` (**NEW**)
- `src/blocks/TourType/config.ts` (เปลี่ยนชื่อ labels)
- `src/blocks/RenderBlocks.tsx`, `src/collections/Pages/index.ts` (ลงทะเบียน Block ใหม่)
- `src/heros/PostHero/index.tsx`, `src/utilities/generateMeta.ts` (TS fix)

### 2026-03-31 — [fix] แก้ไขเมนูทัวร์ต่างประเทศ, ภาษาอังกฤษในช่องค้นหา และ URL Redirect

**แก้ไขโครงสร้าง Header / Mega Menu:**
- แก้ปัญหาเมนู "ทัวร์ต่างประเทศ" หาย (หรือแสดงข้อความแปลกๆ อย่าง `Tour 69a...`) ที่เกิดจากข้อจำกัด Payload CMS Pagination / Hydration Limit
- ปรับเปลี่ยนโครงสร้างใน `Header/Component.tsx` ให้มีระบบรวบรวมรหัส ID ที่ดึงไม่สมบูรณ์มารอคิว แล้วทำการกวาดข้อมูลอัปเดตทัวร์ใหม่ทั้งหมด (Hydration) โดยวิ่งตรงไปหา `intertours` และ `inbound-tours` เพื่อแสดงผลชื่อ+ธงให้ครบทุกแถว
- ใช้สคริปต์แก้ไขฐานข้อมูลเพื่อคลีนรหัสทัวร์ต่างประเทศที่สูญหาย และกู้คืนโครงสร้าง `inbound-tours` (ทัวร์ภาคต่างๆ) แบบสมบูรณ์ด้วยการใช้ข้อมูลสำรองจากไฟล์ `header_export.json`
- ปรับปรุงการเซฟค่าเริ่มต้น (`depth: 1`) ใน `navbar.config.ts` เลี่ยงปัญหาทัวร์ขึ้นไม่ครบ 

**เพิ่มแสดงผลภาษาอังกฤษในช่อง Dropdown ค้นหาประเทศ:**
- ปรับแก้ Search Dropdown ในหน้า `intertours` และ `inbound-tours` ให้แสดงผลคู่ชื่อประเทศแบบ `ทัวร์ญี่ปุ่น (Japan)`
- อัปเดต Type ภายใน Component ให้รองรับ `title_en` เรียบร้อย ไม่ติด TypeScript error

**เพิ่มกฎ Redirect เส้นทาง URL (ครอบคลุมครบทุกกรณี):**
- เพิ่มกฎดักการพิมพ์ URL หรือเชื่อมลิงก์ผิดเป็น `/intertour` ให้เด้งไปที่ `/intertours` อัตโนมัติใน `next.config.ts`
- อัปเดตกฎเก่าของ `/tour` โดยใช้ Path Matching (`/tour/:path*` และ `/tours/:path*`) เพื่อควบรวมทุกกรณี (เช่น `/tour/ประเทศ` หรือ `/tours/ประเทศ`) ให้วิ่งตรงเข้า `/intertours/ประเทศ` เพื่อลด Redirect Hops และจบปัญหา URL เลื่อนลอย

**แยกแก้ไขไฟล์แว่นขยาย (Search Heading):**
- เปลี่ยนการใช้ Base64 ไอคอนรูปแว่นขยายในแถบ Search (หน้าแรก) ไปใช้รูปจาก PageConfig แทน (`headingIcon`) ป้องกันปัญหาไฟล์เสียจากการฝัง

**ไฟล์ที่เกี่ยวข้อง:**
- `src/globals/Header/Component.tsx`, `src/globals/Header/navbar/blocks/TourCategoryMenu.tsx`, `src/globals/Header/navbar/navbar.config.ts`
- `src/app/(frontend)/intertours/[slug]/CountrySearchResults.tsx`, `src/app/(frontend)/inbound-tours/[slug]/CountrySearchResults.tsx`
- `next.config.ts`
- `src/blocks/SearchTour/wowtour_search1.tsx`, `src/utilities/getSearchOptions.ts`

### 2026-03-30 — [feat] Migrate SeedDB to S3 & Restrict Access

**SeedDB S3 Migration:**
- ย้ายระบบจัดเก็บ Seed File (`seed-data.tar.gz`) จาก local Git Index ไปยัง S3 (Wasabi)
- เปลี่ยน `applySeed` และ `generateSeed` ให้ดึง/ดันไฟล์จาก S3 อัตโนมัติ (ไม่ผูกกับ `public/media/` หรือ `.git` อีกต่อไป)
- อัปเดต UI ของ SeedDB Panel ให้เปลี่ยนคำอธิบายจากการใช้ Git (main, push, pull) เป็นการใช้ S3
- เพิ่ม `public/seed/` ลงใน `.gitignore` และสั่ง untrack ไฟล์เพื่อลดขนาด Repo ในอนาคต
- เหตุผล: แก้ปัญหา Git repository บวม (ขนาด > 2GB) จากการ track ไฟล์ binary ขนาดใหญ่

**SeedDB Admin Restriction & Cleanup:**
- จำกัดสิทธิ์ SeedDB Dashboard Panel ให้แสดงเฉพาะผู้ใช้ที่มี Role เป็น `admin` เท่านั้น ป้องกันความผิดพลาด
- แก้บักตัวจัดการ Seed ให้กรอง `_versions` collections ออก เพื่อลดขนาดและไม่ให้รก UI

**ไฟล์ที่เกี่ยวข้อง:**
- `src/components/AdminDashboard/SeedDB/actions.tsx`
- `src/components/AdminDashboard/SeedDB/index.tsx`
- `src/components/AdminDashboard/SeedDB/index.client.tsx`
- `scripts/seed.ts`
- `docs/=task/TASK-004_seed_data_storage.md`
- `.gitignore`

### 2026-03-30 — [refactor] ปรับปรุง Unified Search Interface + CMS Schema Cleanup

**เรียง field ตามหลังบ้าน (CMS Backend Order):**
- เรียงลำดับ search fields ทั้ง `SearchResults.tsx` (หน้า search-tour) และ `wowtour_search1.tsx` (หน้าแรก) ให้ตรงกับ CMS: ประเทศ → เทศกาล → สายการบิน → คำค้นหา → Wholesale → ช่วงเวลาเดินทาง → เดือน → ช่วงราคา
- ใช้ `repeat(n, minmax(0, 1fr))` แทน varying widths (0.7fr, 1.3fr) แก้ปัญหา field ทับกัน
- MAX_INLINE = 5 — ≤5 field แสดง inline, ≥6 field ย้ายส่วนเกินไป Advanced Filter อัตโนมัติ

**CMS Schema Cleanup (`PageConfig/config.ts`):**
- ลบ `step` field ออกจาก priceRangeField (ไม่ใช้แล้ว) — hardcode step=1000 ใน frontend 4 ไฟล์
- ลบ `cityField` group ออก — เมืองถูกรวมเข้า countryField ผ่าน checkbox `includeCity` แล้ว
- จัด Layout หลังบ้าน: Label + Placeholder ของ countryField → `type: 'row'` (50/50)
- จัด Layout หลังบ้าน: ราคาต่ำสุด + ราคาสูงสุด ของ priceRangeField → `type: 'row'` (50/50)

**Keyword Search Fix:**
- ย่อ placeholder คำค้นหาจาก "เช่น รหัสทัวร์ / โปรแกรม / สถานที่ท่องเที่ยว" → "รหัสทัวร์, โปรแกรม, สถานที่"

**Price Range — DB Primary + CMS Fallback:**
- ทั้ง home + search-tour ดึง min/max จาก DB API เป็นหลัก (ปัจจุบัน min: 4,888 / max: 1,425,000)
- ถ้า API error หรือไม่มีข้อมูล → fallback ตาม CMS (min: 15,000 / max: 600,000)

**ไฟล์ที่แก้:**
- `src/app/(frontend)/search-tour/SearchResults.tsx`
- `src/blocks/SearchTour/wowtour_search1.tsx`
- `src/app/(frontend)/inbound-tours/[slug]/CountrySearchResults.tsx`
- `src/app/(frontend)/intertours/[slug]/CountrySearchResults.tsx`
- `src/globals/PageConfig/config.ts`

- เปลี่ยนค่าสำรอง (Fallback) ของสีพื้นหลังแถบ Dropdown ตัวเลือกย่อย ใน `wowtour_navbar[1-7].tsx` จาก `inherit` (ที่บางครั้งได้ค่าพื้นหลังแบบใสหลุดรอดมา) ให้เป็นสีขาว `#fff` 
- ดักจับ Error 404 ของ HTTP Request เวลาที่ CMS หาเอกสาร Tour Group ไม่เจอใน `TourType/Component.tsx` และปรับให้แค่พ่นออก `console.warn` แจ้งเตือนใน Terminal แทน เพื่อไม่ให้เกิด Error หน้าแดงบน Next.js Dev Server
- รันอัปเดตไฟล์ `public/seed/seed-data.tar.gz` ให้เป็นโครงสร้างตั้งค่าปัจจุบัน
- สมบูรณ์ Checklist งานแผนการแก้ปัญหา TypeScript ใน `claude_plan/260326_1600_fix_ts_errors.md`
- ไฟล์ที่แก้: `src/blocks/TourType/Component.tsx`, `src/globals/Header/navbar/wowtour_navbar1-7.tsx`, `public/seed/seed-data.tar.gz`

### 2026-03-27 — [feat] Activity Log (Audit Trail) + Payload Versions

**Activity Log System:**
- สร้าง `AuditLogs` collection — immutable, เก็บ log สร้าง/แก้ไข/ลบ/login ทุก collection + globals
- Fire-and-forget pattern: ใช้ `setImmediate()` + direct MongoDB insert ไม่ block request
- สร้าง `withAuditHooks()` wrapper ครอบ 18 collections อัตโนมัติ
- สร้าง `ExportAuditLogsButton` — ปุ่ม Export CSV ใน admin (Payload-consistent design)
- เพิ่ม `/api/audit-logs/export` endpoint (CSV, กรองตามวันที่/action/collection)
- เพิ่ม `canViewAuditLogs` permission ใน Roles — รองรับ role-based access
- เพิ่ม `afterLogin` hook ใน Users สำหรับ login tracking
- ไฟล์ใหม่: `src/collections/AuditLogs.ts`, `src/hooks/auditLog.ts`, `src/utilities/withAuditHooks.ts`, `src/app/api/audit-logs/export/route.ts`, `src/components/ExportAuditLogsButton.tsx`
- ไฟล์ที่แก้: `src/payload.config.ts`, `src/collections/Roles/index.ts`, `src/collections/Users/index.ts`

**Payload Versions:**
- เปิด `versions: { drafts: false, maxPerDoc: 5 }` บน 10 collections: InterTours, InboundTours, CustomLandingPages, GalleryAlbums, Festivals, Testimonials, TourCategories, Categories, Tags, TourGroups
- ไม่เปิดบน ProgramTours (API), Bookings (form), Airlines (API), Media, Users, Roles

**แก้ไข Font:**
- แก้ font paths จาก `node_modules/@fontsource` → `public/fonts` ใน `layout.tsx`

### 2026-03-26 — [chore] สำรวจระบบ Activity Log + ลบ Mock Images

**Activity Log (สำรวจ — ยังไม่ merge):**
- ออกแบบระบบ Activity Log 2 ชั้น: Payload Versions (เก็บ version history) + Custom AuditLogs collection (เก็บ CRUD + Login + View)
- Prototype ไฟล์: `AuditLogs.ts`, `auditLog.ts` (afterChange/afterDelete/afterRead hooks), `trackView.ts` (view throttle 5 นาที)
- ใช้ `withAuditHooks()` wrapper ครอบ 18 collections อัตโนมัติใน `payload.config.ts`
- พบปัญหา: field ชื่อ `collection` เป็น Mongoose reserved keyword → แก้เป็น `targetCollection`
- **สถานะ: Revert แล้ว** — user ต้องการทบทวนรูปแบบก่อน merge, ไฟล์ prototype ยังเก็บไว้ untracked

**Mock Images Cleanup:**
- ลบ `public/mock-images/promo_tours/` (6 ไฟล์) — ไม่ได้ใช้ใน production

### 2026-03-25 — [feat] Locale Migration: EN → TH (ADR-002)

- เปลี่ยน `defaultLocale` จาก `'en'` → `'th'` ใน `src/localization.config.ts`
- ซ่อน locale selector ใน Admin UI ด้วย CSS injection (`src/components/AdminCSS/AgentStarterCSS.tsx`)
- สร้าง migration scripts: `force-globals-th.js`, `fix-breadcrumbs-th.js`, `fix-pages-locale.js`
- ADR: `docs/decisions/002-locale-migration-en-to-th.md`
- ไฟล์ที่แก้: `src/localization.config.ts`, `src/components/AdminCSS/AgentStarterCSS.tsx`

### 2026-03-25 — [chore] Rotate API Secrets & Security Review

- Rotate PAYLOAD_SECRET, SMTP, DRAFT_SECRET, REVALIDATION_KEY, CRON_SECRET
- Add Pre-commit hook to prevent .env leaks
- Update `/admin/globals/api-setting` to use new keys
- Resolve #45 conflicts and merge PR
- Update `package-lock.json`
- Add documentation for TASK-001 (Security)
- Add `.husky` to `tsconfig` exclude
- Fix `site-map/page` error in `tsc` by deleting old file `src/app/(frontend)/site-map/page.tsx`
- Rename /sitemap to /site-map to resolve Vercel build conflict

### 2026-03-25 — [fix] Hero Breadcrumbs ดึงจาก Page อัตโนมัติ

- ลบ `breadcrumbs` array ซ้ำออกจาก hero config (`src/heros/config.ts`) — เดิมต้องกรอกซ้ำกับ nested-docs
- แก้ `wowtour_heroBannerStaticPage1.tsx` + `wowtour_heroBannerStaticPage2.tsx` ให้ใช้ `breadcrumbItems` (page-level) เป็นหลัก
- เพิ่ม `pageTitle` fallback — ถ้า `heroHeading` ว่าง (locale th ไม่มีค่า) ใช้ `title` ของ page แทน
- ไฟล์ที่แก้: `src/heros/config.ts`, `src/heros/PageHero/wowtour_heroBannerStaticPage1.tsx`, `wowtour_heroBannerStaticPage2.tsx`

### 2026-03-25 — [fix] Breadcrumb Validation ไม่ 404 ผิดๆ

- แก้ validation ใน `data.tsx` — breadcrumbs = `null` (ยังไม่สร้าง) ≠ path mismatch
- เดิม: null breadcrumbs → `parentPath = []` → 404 ผิด
- ใหม่: null breadcrumbs → ข้ามการเช็ค, breadcrumbs มีแต่ path ไม่ตรง → ยัง 404
- ไฟล์ที่แก้: `src/app/(frontend)/[[...slugs]]/data.tsx`

### 2026-03-25 — [fix] Preview URL ไม่ hardcode locale

- แก้ Globals config ที่ hardcode `locale: 'en'` → ใช้ `localization.defaultLocale`
- ไฟล์ที่แก้: `src/globals/Header/config.ts`, `src/globals/Footer/config.ts`, `src/globals/ThemeConfig/config.ts`

### 2026-03-25 — [fix] SeedDB E11000 + ลบ field ซ้ำ Festivals

- แก้ SeedDB bulk write ให้ใช้ custom unique keys (`airlineCode`, `slugHoliday`, `groupKey`) แทน `_id`
- ลบ duplicate `title` field จาก Festivals collection — ใช้ `nameHoliday` เป็นหลัก
- ไฟล์ที่แก้: `src/components/AdminDashboard/SeedDB/actions.tsx`, `src/collections/Festivals.ts`

### 2026-03-24 — [refactor] แก้ Hardcoded Inline Styles ทั้งโปรเจค

- ตรวจพบและแก้ inline styles 7 จุดใน frontend:
  - `LandingSearchResults.tsx` — สร้าง `custom-tags.css` แทน inline styles ทั้ง `CustomTagsSection`
  - `wowtour_tourCard1.tsx` — discount badge + link → `.tour-discount-badge` + `.tour-header-link`
  - `TourDetailClient.tsx` — gradient ปุ่มจอง → `.tour-detail-booking-btn`
  - `BookingClient.tsx` — `#e53e3e` → `text-destructive`
  - `ThankYouClient.tsx` — `marginTop: '6px'` → `mt-1.5`
  - `SearchResults.tsx` + `CountrySearchResults.tsx` — `boxShadow` → Tailwind arbitrary value
- แก้ bug `hsl(var(--primary))` ซ้อนกัน — ลบ `hsl()` wrapper ออกเพราะ CSS variables เป็น full color values อยู่แล้ว
- ไฟล์ที่แก้: `custom-tags.css`, `wowtour_tourCard1.css`, `tourDetail.css`, `BookingClient.tsx`, `ThankYouClient.tsx`, `SearchResults.tsx`, `CountrySearchResults.tsx`

### 2026-03-24 — [feat] Custom Landing Page เพิ่ม Sidebar Filter

- เพิ่ม `TourSidebarFilter` ใน `LandingSearchResults.tsx`
- ดึง filter config จาก `/api/globals/page-config` + `/api/search-options`
- ไฟล์ที่แก้: `src/app/(frontend)/custom-landingpage/[slug]/LandingSearchResults.tsx`

### 2026-03-24 — [chore] ลบไฟล์ที่ไม่จำเป็นออกจากโปรเจค

- ลบ 40+ ไฟล์: temp files (`NUL`, `conflicts.txt`, `ts_errors.log`), test files (`test-*.ts`, `test-*.csv`), JSON data dumps, one-off debug scripts (`query-mongo*.ts`, `check_header.js`, `log-menu*.ts` ฯลฯ), one-off scripts ใน `scripts/` (เก็บเฉพาะ `seed.ts`)
- ตรวจแล้วไม่มี import/reference จาก `src/`, `package.json`, `tsconfig.json`

### 2026-03-24 — [docs] อัปเดต Architecture Docs

- `collections.md` — เพิ่ม 4 collections ใหม่: CustomLandingPages, Airlines, Festivals, InboundTours
- `external-api.md` — แยก section หน้าเว็บที่ใช้ CMS vs External API ชัดเจน, เพิ่ม `search-landing` route + `mapProgramTourToCard` utility

### 2026-03-24 — [feat] เพิ่ม Validation เบอร์โทรในฟอร์มจอง + redirect route เก่า + แก้ badge ซ้ำ

**Phone Validation (Booking Form):**
- เพิ่ม validation เบอร์โทรศัพท์: รับเฉพาะตัวเลข, จำกัดไม่เกิน 10 หลัก
- เพิ่ม `inputMode="numeric"` เพื่อแสดง keyboard ตัวเลขบนมือถือ
- เพิ่ม validation ตอน submit: ต้องเป็นตัวเลข 9-10 หลัก
- ไฟล์ที่แก้: `src/app/(frontend)/intertour/.../booking/BookingClient.tsx`, `src/app/(frontend)/tour/.../booking/BookingClient.tsx`

**Route Redirect `/tour/` → `/intertour/`:**
- เพิ่ม permanent redirect (301) ใน `next.config.ts` สำหรับ route เก่า `/tour/` ไปยัง `/intertour/`
- ครอบคลุม: tour detail, booking, thank-you, tag search
- ไฟล์ที่แก้: `next.config.ts`

**แก้ Badge ชื่อประเทศซ้ำ:**
- แก้ปุ่ม badge ในหน้า tour detail ที่แสดง "ทัวร์ทัวร์สิงคโปร์" → "ทัวร์สิงคโปร์"
- สาเหตุ: code เขียน `ทัวร์{countryName}` ซ้ำกับข้อมูลจาก CMS ที่มี "ทัวร์" นำหน้าอยู่แล้ว
- ไฟล์ที่แก้: `src/app/(frontend)/intertour/.../TourDetailClient.tsx`

### 2026-03-24 — [chore] Merge main เข้า tour/mangpor

- Merge PR #38 (fix TS errors navbar5) และ PR #40 (เปลี่ยน route `/tour/` → `/intertour/`, custom modal SyncButton) จาก Tee
- ไม่มี conflict, 22 ไฟล์ (+5,219 บรรทัด)
- ฟีเจอร์ที่เข้ามา: Tour Detail page, Booking page, Thank You page, Search pages, TourStickyShare, SyncButton modal

### 2026-03-20 — [feat] TourType Block เปลี่ยนจาก API → เลือก Tour Group จาก CMS

- เปลี่ยน field `apiTourType` (text กรอก type เช่น "recommend", "group1") → `tourGroup` (relationship เลือกจาก `tour-groups`)
- Admin เลือก Tour Group จาก dropdown → ระบบดึงทัวร์จาก CMS collection แทนเรียก external API
- เพิ่ม `filterOptions` กรองเฉพาะ Tour Group ที่ `isActive = true` ใน dropdown
- เพิ่ม `displayTitle` field ใน TourGroups ให้แสดงชื่อ + groupKey เช่น "ทัวร์ยอดนิยม (group2)" ใน dropdown
- เพิ่ม function `mapProgramTourToTourItemCMS()` สำหรับ map ProgramTour (CMS) → TourItem shape ที่ tour cards ใช้
- ไฟล์ที่แก้: `src/blocks/TourType/config.ts`, `Component.tsx`, `src/collections/TourGroups.ts`, `src/utilities/fetchTourProducts.ts`

### 2026-03-20 — [fix] แก้ Header scroll, box-shadow, active state + Gallery image fill

**Header Box-Shadow (ทุก version):**
- เพิ่ม box-shadow ให้แถบเมนูทั้ง non-sticky และ sticky state: `0 4px 20px rgba(0, 0, 0, 0.15)` (Header 5 ใช้ `0.06`)
- เพิ่ม `position: relative; z-index: 1` ให้ Header 6 navstrip เพื่อให้ shadow แสดงเหนือ content ด้านล่าง
- ไฟล์ที่แก้: `wowtour_navbar1-7.css`

**Header Scroll Transition:**
- แก้ scroll jitter ของ Header 6 และ 7 — ย้าย `top-wrapper` ออกจาก `StickyNavDetector` ให้ scroll หายไปตามปกติ
- เพิ่ม `fadeInMenu` animation ให้ Header 3 เหมือน Header 1
- ไฟล์ที่แก้: `wowtour_navbar6.tsx`, `wowtour_navbar7.tsx`, `wowtour_navbar3.css`

**NavLinkActive — รองรับ Header 5-7:**
- แก้ `NavLinkActive.tsx` — เพิ่ม selector สำหรับ Header 5, 6, 7 ที่ขาดหายไป ทำให้ active state ไม่ทำงาน
- แก้ Header 5 CSS — เปลี่ยนจาก `var(--primary)` เป็น `var(--header-accent, var(--primary))` ให้อ่านค่าสีจาก admin field
- ไฟล์ที่แก้: `NavLinkActive.tsx`, `wowtour_navbar5.css`

**Media Component — แก้ภาพไม่เต็มกรอบ:**
- เพิ่ม `object-fit: cover` เป็น default เสมอสำหรับ external URL images (เดิมไม่ตั้ง `objectFit` เมื่อมี `imgClassName`)
- เพิ่ม `width: 100%; height: 100%; position: relative` ให้ wrapper div เมื่อใช้ `fill` prop
- ไฟล์ที่แก้: `src/components/Media/ImageMedia/index.tsx`, `src/components/Media/index.tsx`

**Gallery Listing — Grid auto-fit:**
- เปลี่ยน grid จาก `repeat(4, 1fr)` เป็น `repeat(auto-fit, minmax(180px, 1fr))` รองรับจำนวน albums ไม่จำกัด
- ไฟล์ที่แก้: `src/blocks/GalleryListing/wowtour_galleryListing1.css`

**Gallery Album 5 — แก้ label:**
- เพิ่ม `\n` ให้ชื่อ Gallery Album 5 ขึ้นบรรทัดใหม่หลังเลข version
- ไฟล์ที่แก้: `src/blocks/GalleryAlbum/config.ts`

**ProgramTours Collection (ใหม่):**
- สร้าง collection `programtour` เก็บโปรแกรมทัวร์จาก API (ราคา, สายการบิน, ช่วงเดินทาง, ประเทศ, เมือง, ทวีป)
- สร้างปุ่ม **"🔄 Sync Products จาก API"** ใน admin — ดึง products ทุกหน้า (pagination ทีละ 50) + upsert ด้วย `productCode`
- เพิ่มฟิลด์ `continent` (ทวีป) — หาอัตโนมัติจาก InterTours → TourCategory ตอน sync
- Data Transformation: เพิ่ม "ทัวร์" นำหน้า `countryName` และ `cityName`
- ไฟล์ที่เพิ่ม: `src/collections/ProgramTours.ts`, `src/app/(frontend)/api/sync-program-tours/route.ts`, `src/components/SyncProgramToursButton.tsx`, `src/components/SyncProgramToursButton.css`

**InterTours — รองรับเมือง (parentCountry):**
- เพิ่มฟิลด์ `parentCountry` (self-reference → intertours) เพื่อเชื่อมเมืองกับประเทศหลัก
- เมืองสืบทอด `category` จากประเทศหลักอัตโนมัติ

**Import CSV — ปรับปรุง:**
- เพิ่มตัวเลือก mapping: `category_slug` (slug ภาษาอังกฤษ), `parentCountry` (TH), `parentCountry_en` (EN)
- Category ซ้ำ → อัปเดตทับ (title + slug + order)
- parentCountry ไม่เจอ → สร้างประเทศหลักใหม่อัตโนมัติจาก TH + EN
- Display Order ใน TourCategories เรียงตามลำดับ import
- แก้ bug: ลบ fallback ที่เอา `ParentCountryName_TH` ไปทับ Category
- แก้ bug: ใช้ `locale: 'en'` (default locale) แทน `'th'` ป้องกัน "Untitled" ใน TourCategories

- ไฟล์ที่แก้: `src/collections/InterTours.ts`, `src/app/api/import-intertours/route.ts`, `src/components/ImportInterToursButton.tsx`

### 2026-03-18 — [feat] เพิ่ม Unified Proxy Route + Link API Mode tab

- สร้าง `/api/wowtour` — Unified Proxy Route เส้นเดียวรองรับทุก API mode (LoadCountry, searchresultsproduct, productdetails ฯลฯ)
- ดึง `apiEndPoint` + `apiKey` จาก Admin Setting อัตโนมัติ ไม่ต้องใส่ key ใน URL
- ใช้ Stale-While-Revalidate cache (5 นาที) — เร็ว + อัพเดตอัตโนมัติ
- แก้ bug URL มี `?` ซ้ำ 2 ตัว เมื่อ `apiEndPoint` ลงท้ายด้วย `?`
- เพิ่ม tab **"Link API Mode"** ใน `/admin/globals/api-setting` — แสดงลิงก์ proxy คลิกได้ + Copy + Copy All สำหรับแชร์กับทีม
- ไฟล์ที่เพิ่ม: `src/app/(frontend)/api/wowtour/route.ts`, `src/components/AdminDashboard/ApiModeLinks/Component.tsx`
- ไฟล์ที่แก้: `src/globals/ApiSetting/config.ts`

### 2026-03-17 — [docs] ปรับโครงสร้าง Documentation
- ปรับโครงสร้าง `docs/` จากโฟลเดอร์ `handover/` เดิม เป็น 4 โฟลเดอร์: `architecture/`, `guides/`, `decisions/`, `changelog/`
- เพิ่ม `docs/architecture/external-api.md` เอกสาร API ภายนอก (Tourprox/apiwow)
- เพิ่ม `docs/architecture/collections.md` อ้างอิง Collections & Globals ของ Payload CMS
- สร้าง `docs/decisions/001-api-cache-layer.md` (ADR แรก)
- สร้าง `.agents/workflows/update-docs.md` สำหรับอัปเดต docs อัตโนมัติ
- ไฟล์ที่แก้: `docs/`, `.agents/workflows/`, `.cursorrules`

### 2026-03-17 — [refactor] ลบ .cursorrules ย้ายไป workflows
- ลบ `.cursorrules` (ใช้เฉพาะ Cursor, ไม่จำเป็นในโปรเจกต์นี้)
- ย้ายกฎ coding conventions ไป `docs/architecture/overview.md`
- สร้าง `/git-push` workflow — แทนที่ Section 6 ใน .cursorrules
- สร้าง `/review-ui` workflow — ให้ AI รีวิว UI/UX อัตโนมัติด้วย ui-ux-pro-max skill
- ไฟล์ที่แก้: `.cursorrules` (ลบ), `docs/architecture/overview.md`, `.agents/workflows/`

### 2026-03-17 — [test] เพิ่มระบบ Automated Testing
- เพิ่ม Playwright config (`playwright.config.ts`) รองรับ Desktop/Tablet/Mobile
- สร้าง test specs: `tests/homepage.spec.ts`, `tests/seo.spec.ts`, `tests/security.spec.ts`
- ทดสอบครอบคลุม: โหลดหน้า, ประสิทธิภาพ (< 1 วินาที), เนื้อหา, รูปภาพ, SEO meta, CSP headers
- เพิ่มคำสั่ง `pnpm test:quick` ใน `package.json`
- ไฟล์ที่แก้: `playwright.config.ts`, `tests/`, `package.json`

### 2026-03-17 — [fix] แก้ TypeScript errors 32 จุด ใน 17 ไฟล์
- แก้ `Header.logo` ที่หายไปจาก type ใน 4 ไฟล์ navbar (ใช้ `(header as any).logo`)
- แก้ `getCachedGlobal` return type กว้างเกินไปใน `Footer/Component.tsx` และ `layout.tsx`
- เปลี่ยนชื่อ `WowtourSearchPageBlock` → `WowtourSearchTourBlock` ตาม payload-types ใหม่
- เพิ่ม table/tablerow/tablecell node types ใน `RichText/serialize.tsx`
- แก้ `deepMerge` type widening ใน `fields/link.ts`
- แก้ property `color` ซ้ำใน `ApiUrlPreview/Component.tsx`
- แก้ type errors ย่อยในไฟล์อื่นๆ: `About`, `BlogCard`, `fix-slugs`, `query-payload`, `check-globals`, `seed`
- ผลลัพธ์: `tsc --noEmit` ผ่าน 0 errors
- ไฟล์ที่แก้: 17 ไฟล์ใน `src/`, `scripts/`, root

### 2026-03-17 — [perf] ปรับ Playwright config ให้เร็วขึ้น
- ลด test timeout จาก 60 วินาที → 10 วินาที, navigation timeout จาก 60 → 5 วินาที
- เปิด parallel execution: `fullyParallel: true`, `workers: 4` (local)
- เปลี่ยน dev server `stdout` จาก `'ignore'` เป็น `'pipe'` เพื่อเห็น error ตอน start
- ผลลัพธ์: test suite เร็วขึ้นจาก ~8 นาที → ~1.6 นาที
- ไฟล์ที่แก้: `playwright.config.ts`

### 2026-03-18 — [feat] ปรับแต่งสี Header และ Social Icons

- เพิ่ม field `topContainerTextColor` สำหรับปรับสีตัวอักษร Container บนสุด (เฉพาะ Header 4, 6)
- เพิ่ม field `hotline` ใน CompanyInfo global + แสดงผลใน Header 6 info bar พร้อม icon Headphones
- เปลี่ยน Social Icons ใน Header 6 เป็น Simple Icons (official brand SVG) — Facebook, LINE, Instagram, TikTok, YouTube, X
- เพิ่ม White, Black เป็นตัวเลือกสีใน GradientPicker + จัดเรียง Primary, Secondary มาก่อน
- เปลี่ยน label `menuActiveColor` → "สีตัวอักษรเมนูกรณี Hover และ Active เมนู"
- แก้ CSS fallback ของ info bar ให้รองรับสีเดี่ยว (`--header-gradient-full` → `--header-gradient` → fallback)
- เพิ่ม `border-bottom` ให้ Container บนสุดของ Header 6
- สร้าง CSS variable `--header-top-text` สำหรับสีตัวอักษร Container บนสุด (ใช้ใน H4, H6)
- เพิ่ม `image` property ให้ Header 6 ใน design version selector (รูปมีอยู่แล้วแต่ลืมอ้างอิง)
- เพิ่มรูป preview Header 6 ใน `docs/guides/theme-config.md`
- ไฟล์ที่แก้: `src/globals/Header/config.ts`, `Component.tsx`, `wowtour_navbar6.tsx`, `wowtour_navbar6.css`, `wowtour_navbar4.css`, `src/globals/CompanyInfo/config.ts`, `src/fields/gradientPicker/GradientPickerComponent.tsx`
